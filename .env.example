# Romulus Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# Port Configuration
# =============================================================================
# Frontend development server port
FRONTEND_PORT=3000

# Backend API server port
BACKEND_PORT=8000

# PostgreSQL database port (external)
POSTGRES_PORT=5432

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL credentials
POSTGRES_USER=romulus
POSTGRES_PASSWORD=romulus
POSTGRES_DB=romulus

# Database URL for backend (uses internal Docker network)
# Note: The host 'postgres' refers to the Docker service name
# When running backend locally (not in Docker), use localhost:${POSTGRES_PORT}
DATABASE_URL=postgresql+asyncpg://romulus:romulus@postgres:5432/romulus

# =============================================================================
# Application Configuration
# =============================================================================
# Backend API base URL for frontend
# Uses BACKEND_PORT variable
REACT_APP_API_URL=http://localhost:${BACKEND_PORT}

# CORS allowed origins (frontend URL)
# Uses FRONTEND_PORT variable
FRONTEND_URL=http://localhost:${FRONTEND_PORT}

# Debug mode
DEBUG=false

# =============================================================================
# Development Notes
# =============================================================================
# - Change FRONTEND_PORT, BACKEND_PORT, and POSTGRES_PORT to run multiple
#   instances of the application concurrently without port conflicts
# - REACT_APP_API_URL and FRONTEND_URL automatically use the port variables
# - DATABASE_URL uses 'postgres:5432' (internal Docker network) - this stays
#   the same regardless of POSTGRES_PORT, which only affects external access
# - Example for running second instance: set FRONTEND_PORT=3001, BACKEND_PORT=8001, POSTGRES_PORT=5433
